{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug main.rs with args",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/antares-rpc-client",
            "args": [
                "client_config.json",
            ],
            "cwd": "${workspaceFolder}",
            "cargo": {
                "args": [
                    "build"
                ]
            }
        }
    ]
}